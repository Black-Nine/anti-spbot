do

function run(msg, matches)
  return ' lammerTG V1.0.0 \nCREATED BY (MR.LAMMER)[TELEGRAM.ME/MRLAMMER]'
end

return {
  patterns = {
    "^#version$"
    "^#LammerTG$"
    "^#ورژن$"
  }, 
  run = run 
}

end
